const { Network } = require("frost-devkit");
const { Bitcoin, Regtest, Testnet, Signet} = require("bitcoinjs-lib");

module.exports = {
    Bitcoin,
    Regtest,
    Testnet,
    Signet,
    Network
}

export {
    Bitcoin,
    Regtest,
    Testnet,
    Signet,
    Network
}

